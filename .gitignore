# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Secrets — never commit environment files.
# Only VITE_-prefixed vars reach the client bundle and are therefore public;
# anything else (e.g. a Supabase service_role key) must stay out of the repo.
.env
.env.*
!.env.example

# Local tooling
.claude/settings.local.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
