# Copy to .env and fill in. Only VITE_-prefixed variables are exposed to the
# client bundle, so never put a server-side secret (e.g. a Supabase
# service_role key) in this file.

# TMDB read access token. Only needed by the legacy /movie/:id prototype route.
VITE_TMDB_API_KEY=
